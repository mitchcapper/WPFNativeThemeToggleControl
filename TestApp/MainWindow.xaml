<Window x:Class="TestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestApp"
		xmlns:sw="clr-namespace:System.Windows;assembly=PresentationFramework"
		xmlns:ThemeToggle="clr-namespace:WPFNativeThemeToggleControl;assembly=WPFNativeThemeToggleControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="400">
    <Grid>
		<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
			<ThemeToggle:ThemeToggleControl x:Name="ThemeToggle" CurrentTheme="{x:Static sw:ThemeMode.Dark}"
                                    ThemeChanged="ThemeToggle_ThemeChanged"
                                    HorizontalAlignment="Right"
                                    Margin="5" />
		<TextBox BorderThickness="2"  Margin="5" Padding="5" Width="300" Text="Input" />
			<Button Content="InpButton" Margin="5" Padding="5" />
		</StackPanel>
	</Grid>
</Window>
